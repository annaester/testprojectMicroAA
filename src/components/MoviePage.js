import React, { useState, useEffect } from "react";

const MoviePage = () => {
  const [moviePage, setMoviePage] = useState({});

  //   useEffect(() => {
  //     fetch()
  //       .then((res) => res.json)
  //       .then((data) => setMoviePage(data));
  //   });

  return <>Hello from moviepage</>;
};

export default MoviePage;
